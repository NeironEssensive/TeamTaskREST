version: '3.8'

services:

  redis:
    image: redis:7-alpine
    container_name: teamtask-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass teamtask_redis_pass
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data: